<%= form_for [@challenge, @step] do |f| %>
  <% if step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<!-- **** STEP ONE ******* -->
<div class="step-one">
  <div class="hidden-field">
    <%= f.hidden_field :challenge_id %>
    <%= f.hidden_field :challenge_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :completed %>
    <%= f.check_box :completed %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
</div>
  <!-- **** STEP TWO ******* -->
  <div class="step-two">
    <div class="hidden-field">
      <%= f.hidden_field :challenge_id %>
      <%= f.hidden_field :challenge_id %>
    </div>

    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label :completed %>
      <%= f.check_box :completed %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  </div>

    <!-- **** STEP THREE ******* -->
    <div class="step-three">
      <div class="hidden-field">
        <%= f.hidden_field :challenge_id %>
        <%= f.hidden_field :challenge_id %>
      </div>

      <div class="field">
        <%= f.label :name %>
        <%= f.text_field :name %>
      </div>

      <div class="field">
        <%= f.label :completed %>
        <%= f.check_box :completed %>
      </div>

      <div class="actions">
        <%= f.submit %>
      </div>
    </div>

      <!-- **** STEP FOUR ******* -->
     <div class="step-four">
        <div class="hidden-field">
          <%= f.hidden_field :challenge_id %>
          <%= f.hidden_field :challenge_id %>
        </div>

        <div class="field">
          <%= f.label :name %>
          <%= f.text_field :name %>
        </div>

        <div class="field">
          <%= f.label :completed %>
          <%= f.check_box :completed %>
        </div>

        <div class="actions">
          <%= f.submit %>
        </div>
      </div>

        <!-- **** STEP FIVE ******* -->
        <div class="step-five">
          <div class="hidden-field">
            <%= f.hidden_field :challenge_id %>
            <%= f.hidden_field :challenge_id %>
          </div>

          <div class="field">
            <%= f.label :name %>
            <%= f.text_field :name %>
          </div>

          <div class="field">
            <%= f.label :completed %>
            <%= f.check_box :completed %>
          </div>

          <div class="actions">
            <%= f.submit %>
          </div>
        </div>

          <!-- **** STEP SIX******* -->
          <div class="step-six">
            <div class="hidden-field">
              <%= f.hidden_field :challenge_id %>
              <%= f.hidden_field :challenge_id %>
            </div>

            <div class="field">
              <%= f.label :name %>
              <%= f.text_field :name %>
            </div>

            <div class="field">
              <%= f.label :completed %>
              <%= f.check_box :completed %>
            </div>

            <div class="actions">
              <%= f.submit %>
            </div>
          </div>
<% end %>
